# Core
streamlit==1.39.0
python-dotenv==1.0.1
pydantic==2.9.2

# Sarvam SDK (or HTTP via requests if SDK not pinned-stable)
requests==2.32.3
httpx==0.27.2

# PDF / document parsing
pypdf==5.0.1
pdfplumber==0.11.4

# RAG: vector store + embeddings client
chromadb==0.5.20

# LLM / embeddings fallbacks (benchmarking only)
openai==1.54.4

# Structured store
duckdb==1.1.3

# Audio
sounddevice==0.5.1
soundfile==0.12.1
numpy==1.26.4

# Utility
pandas==2.2.3
tqdm==4.66.6
tenacity==9.0.0
